# =============================================================================
# WebPulse — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
# Recommended:   Use a virtual environment (venv) first.
# =============================================================================

# --- Core / Scientific Computing & Machine Learning ---
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
torchvision>=0.15
onnxruntime-directml>=1.16 # AMD GPU support via DirectML on Windows

# --- Video / Computer Vision ---
opencv-python>=4.8
mediapipe>=0.10

# --- Audio / Signal Processing ---
librosa>=0.10
sounddevice>=0.4           # Real-time microphone capture
soundfile>=0.12            # Audio file I/O (needed by sounddevice/librosa)
imageio-ffmpeg>=0.4
websockets>=14.0           # Persistent Gemini Live API session

# --- ASR (Speech-to-Text) ---
openai-whisper>=20231117

# --- LLM Client Libraries ---
google-genai>=0.1.0        # Google Gemini Client
google-generativeai>=0.4.0 # Gemini fallback
openai>=1.0                # OpenAI GPT API
anthropic>=0.25            # Anthropic Claude API

# --- TTS (Text-to-Speech) ---
pyttsx3>=2.90              # Local TTS engine (no API key needed)

# --- Environment / Config ---
python-dotenv>=1.0         # Load .env files

# --- Plotting ---
matplotlib>=3.7
